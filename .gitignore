/.xdg/
/.tools/

# Local environment overrides (often contains secrets)
.env

# Claude Code machine-local permissions/config overrides
.claude/settings.local.json

# Beads runtime database artifacts must stay local-only.
.beads/*.db
.beads/*.db-*

# Test coverage artifacts — generated, not source.
/coverage/
